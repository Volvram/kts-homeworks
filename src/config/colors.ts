export enum COLORS {
  neutral = "neutral",
  positive = "positive",
  negative = "negative",
}

export const colorsmap: Record<COLORS, string> = {
  neutral: "#6c757d",
  positive: "#21bf73",
  negative: "#d90429",
};